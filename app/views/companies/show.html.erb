
<div class="courses-container">
	<div class="course">
		<div class="course-preview">
			<h6><%= @company.company_name %></h6>
			<h2>₹<%=@company.premium%></h2>
			  <% if @company.pdf.present?%>
              <%=link_to "View Documentations",rails_blob_path(@company.pdf)%>
        <% end %>
		  </div>
		<div class="course-info">
			<h6><%= @company.policy.title %></h6>
			<h2><%=@company.plan_info %></h2>
            <%if @company.user_companies.present? %>
              <i class="text-success"> *get 10% discount on renewal</i>
            <% end%>
          <div class="card-buttons">
          
            <%if @company.user_companies.present? %>
              <%= button_to 'Renew',renew_path , params: {id: @company.id}, remote: true , class:"btn btn-lg buy-button",id:"renew-button" , disabled: @enable_button_at > Time.now %> 
            <% end%>
            <%= button_to 'Buy Now',checkout_index_path , params: {id: @company.id}, remote: true , class:"btn btn-lg buy-button"%> 
              <% if current_user && current_user.admin? %>
            <a><%= link_to 'Edit', edit_company_path(@company) , class:"btn btn-lg buy-button"%> </a>
              <a><%= button_to 'Delete', company_path(@company), method: :delete , class:"btn btn-lg buy-button"%></a>
            <% end %> 
          </div>
		</div>
	</div>
</div>
